language: python
os: linux
python:
    - 2.7
addons:
    apt:
        packages:
            - libhdf5-serial-dev
            - libopenmpi-dev
            - openmpi-bin
install:
    - "pip install -r requirements.txt"
    - "python setup.py develop"
script:
    - "cd ~; nosetests caput"
    #- "cd ~; mpirun -np 4 nosetests caput"

